# VisualAlarm 設計書

macOS 用のメニューバー常駐アラーム／タイマー。音も通知センターも使わず、**画面の見た目だけ**で知らせる。
この文書が仕様の唯一の正典。実装上の決定は `CLAUDE.md`、経過は `開発記録.md` に残す。

## 1. 方針

- メニューバーに常駐し、Dock には表示しない
- 知らせる手段は画面表示のみ。音・通知センター・バッジは使わない
- 表示中も **キーボード入力を一切奪わない**。ユーザーが文字入力中（IME 変換中を含む）でも作業を壊さない
- 止める・解除する操作は **マウスクリックのみ**。キーボードでは操作できない（誤操作防止のため意図的にそうする）
- 全ディスプレイに同じ表示を出す
- 外部依存なし。macOS 14 以上

## 2. 用語

| 用語 | 意味 |
|---|---|
| アラーム | 「14:30」のように時刻でセットするもの |
| タイマー | 「25 分」のように長さでセットするもの。終了時刻 = セット時刻 + 長さ |
| 設定時刻 | アラームの時刻、またはタイマーの終了時刻 |
| ラベル | 何のためのアラーム／タイマーかを表す文字列（例:「〇〇さんに電話」） |
| カウントダウン | 設定時刻の 5 秒前から始まる 5・4・3・2・1 の全画面表示 |
| 本番表示 | 設定時刻になったときの全画面表示 |
| 履歴 | 過去にセットしたアラーム／タイマーの記録。再セットに使う |

## 3. 機能

### 3.1 アラーム

- セットする項目
  - 時刻（時・分）
  - ラベル（任意。空なら本番表示では「アラーム」と出す）
  - 色（本番表示の背景色。既定は「おまかせ」＝鳴るたびに緑・青系からランダム。固定したいときだけ選ぶ）
  - 繰り返し（既定は「なし」。「毎日」「平日」「曜日を選ぶ」。編集画面では折りたたんだ「詳細」の中に置き、通常は目に入らないようにする）
- 繰り返しなしのアラームは、鳴ったら自動で無効になる（一覧には残り、スイッチで再度有効にできる）
- 繰り返しありのアラームは、鳴った後も有効のまま次回に備える
- 複数登録でき、同時に有効にできる
- 有効／無効はスイッチで切り替える

### 3.2 タイマー

- セットする項目
  - 長さ（時・分・秒）
  - ラベル（任意。空なら本番表示では「タイマー終了」と出す）
  - 色（既定は「おまかせ」）
- セット方法は 2 つ
  1. **プリセット**: ボタンをクリックすると即座に開始。初期値は 1・3・5・10・15・30・45・60 分
  2. **自由入力**: 時・分・秒を入力して開始。開始した長さは **プリセットに追加され、以後ボタンとして残る**（iPhone のタイマーと同じ）
- プリセットはユーザーが削除できる（右クリック → 削除）。並びは長さ順
- 複数のタイマーを同時に走らせられる。実行中のタイマーは一覧に残り時間を表示し、個別にキャンセルできる
- 実行中のタイマーはアプリを終了・再起動しても継続する（終了時刻を保存しておく）

### 3.3 カウントダウン（設定時刻の 5 秒前〜）

- 設定時刻の **5 秒前**に始まり、1 秒ごとに 5 → 4 → 3 → 2 → 1 と進む
- 見た目
  - 背景: **半透明の暗色**（黒 35% 程度）。下の作業画面は透けて見える
  - 数字: **ポップな風船文字**（丸ゴシックの太い字形に、明るいキャンディ色のグラデーション・太い白フチ・ツヤのハイライト・影。色は数字ごとにランダム）。ふくらむように現れる。画面の高さの 70% ほどの大きさで中央に現れ、1 秒かけて **画面中央に吸い込まれるように縮小しながら消える**（縮小＋フェード。0.15 秒で出現、残り 0.85 秒で消える）。消え終わると同時に次の数字が現れる
  - 登場のしかたと消え方は数字ごとに **ランダムに組み合わせる**。単調にならないための遊び
    - 登場: 中央でふくらむ／左・右・上・下から流れ込みながらふくらむ
    - 消え方: 中央に吸い込まれる／左右どちらかにくるっと回りながら吸い込まれる／裏返りながら吸い込まれる／
      **ふくらんで破裂する**（限界まで震えながらふくらみ、破片と白い輪が飛び散る）／
      **しぼみながらジグザグに飛んでいく**（口を離した風船）／**重力で落ちていく**
    - 消え方は均等ではなく重み付き（破裂 25・しぼむ 20・落下 15・吸い込まれる 12・裏返り 10・回転 3 種 各 6）。
      同じ消え方が 2 つ続かないようにする。5 秒のカウントダウンで派手なもの（破裂・しぼむ・落下）が平均 2.6 回出る
  - ラベル: 数字の下に固定表示（動かない）。複数が同時にカウントダウンしているときは縦に並べる
  - 「停止」ボタン: 画面下部中央に固定表示
- 操作
  - **「停止」ボタンをクリック**したときだけ止まる。止めるとそのアラーム／タイマーは鳴らず、本番表示も出ない（繰り返しアラームは次回に備える。1 回のアラームは無効になる。タイマーは消える）
  - 停止ボタン以外の場所へのクリックは **下のアプリにそのまま届く**（作業を邪魔しない、誤クリックで消えない）
  - キーボードは一切拾わない
- カウントダウンの秒数は設定で 3〜10 秒に変えられる（既定 5）

### 3.4 本番表示（設定時刻になったとき）

- 全ディスプレイを覆う。背景はそのアラーム／タイマーの色（不透明度 90%）で **点滅はしない**
- 色は **緑・青系の落ち着いた 7 色**（緑・ミント・ティール・空・青・藍・ラベンダー）。「おまかせ」なら鳴るたびにこの中からランダムに選ぶ。
  赤・オレンジは「危険」を連想させるので使わない
- 表示内容（中央に縦並び）
  - ラベル（画面の主役。最大サイズ。長い場合は 2 行まで縮小して収める）
  - 設定時刻（アラームは「14:30」、タイマーは「25 分のタイマー」）
  - 設定時刻からの経過時間（「3 分経過」。1 分未満は出さない。離席して戻ってきたときの手掛かり）
  - 「解除」ボタン、「スヌーズ（N 分）」ボタン
  - 案内文「画面のどこかをクリックで解除」
- ラベルの文字は **ゆっくり波打つ**（1 文字ずつ位相をずらして上下にうねる。1 周期 2.4 秒、振幅は文字高さの 12% 程度）。背景はごくゆっくり明暗が呼吸する（1 周期 4 秒、±5%）
- 複数が同時に鳴ったときは 1 つの本番表示にまとめ、ラベルを縦に並べる
- 解除するまで出続ける。設定で「自動で解除」（10 秒／30 秒／1 分／5 分）を選べる（既定は自動解除なし）

### 3.5 停止・解除の操作（まとめ）

| 場面 | 操作 | 理由 |
|---|---|---|
| カウントダウン中 | 「停止」ボタンのクリックのみ | 誤クリックで鳴らなくなるのを防ぐ。作業中のクリックは下のアプリに通す |
| 本番表示中 | 画面のどこをクリックしても解除。「解除」ボタン、メニューバーアイコンのクリックでも解除 | 全面に文字が出ている＝クリックしたなら読んでいる |
| 本番表示中 | 「スヌーズ」ボタンで閉じ、N 分後に同じラベルで再度カウントダウン→本番表示 | |
| どちらも | キーボードでは操作できない | IME 変換中の文字が確定したり、作業が中断するのを避ける |

どの表示も **アプリを前面化せず、キーウィンドウにならない**。表示中もキーボード入力は直前まで使っていたアプリに届き続ける。

### 3.6 履歴

- アラームをセットするたびに「ラベル＋時刻」を、タイマーを開始するたびに「ラベル＋長さ」を履歴に残す
- 同じラベル＋同じ時刻（長さ）はまとめて 1 件にし、最後に使った日時を更新する。並びは最後に使った順
- 上限は種別ごとに 20 件。超えた分は古いものから消す
- 履歴の項目をクリックすると、そのラベル・時刻（長さ）・色で即座にセットする（アラームは有効な状態で一覧に追加、タイマーは開始）
- 履歴は個別に削除できる（右クリック → 削除）、または「履歴を消去」で全消し
- 一覧の同じ項目を何度もセットするより、履歴から選ぶのが基本の使い方になる想定（特にアラーム）

### 3.7 メニューバー

- アイコンは目覚まし時計。有効なアラームか実行中のタイマーがあるときは塗りつぶし、なければ線画
- 実行中のタイマーがあれば、終了が一番近いものの残り時間（mm:ss、1 時間以上は h:mm:ss）をアイコンの右に出す（設定でオフにできる）
- クリックでパネルが開く。本番表示中にクリックした場合はパネルを開かずに解除だけ行う

### 3.8 設定

| 項目 | 既定 | 範囲 |
|---|---|---|
| 標準の色 | おまかせ | おまかせ（緑・青系からランダム）／緑・ミント・ティール・空・青・藍・ラベンダー |
| カウントダウンの秒数 | 5 | 3〜10 |
| カウントダウンの背景の濃さ | 35% | 10〜70% |
| 本番表示の不透明度 | 90% | 50〜100% |
| スヌーズ | 5 分 | 1・3・5・10・15 分 |
| 自動で解除 | しない | しない／10 秒／30 秒／1 分／5 分 |
| メニューバーに残り時間を表示 | オン | |
| ログイン時に起動 | オフ | |
| 言語 | システムに従う | システムに従う／日本語／English／简体中文／繁體中文／한국어 |

### 3.9 言語

- UI の文言は日本語・英語・中国語（簡体字・繁体字）・韓国語。既定では macOS の言語設定に従い
  （ja → 日本語、zh-Hans/zh-CN → 簡体字、zh-Hant/zh-TW/zh-HK/zh-MO → 繁体字、ko → 韓国語、それ以外 → 英語）
- 設定でいつでも切り替えられる。切り替えは即座に反映され、再起動は不要
- 実装は英語の文言をキーにした辞書方式（`Logic/Localization.swift`）。文言を足すときは英語をそのまま書き、各言語の訳を辞書に追加する
- 数値と単位の間の空白、時間の部品の区切り、曜日の並べ方は言語ごとに変える（英語 "1 h 30 min" / 日本語・中国語 "1時間30分" / 韓国語 "1시간 30분"）

## 4. 画面（メニューバーのパネル）

幅 360pt。上から順に:

1. ヘッダー: アプリ名、「次: 明日 07:30」の表示、設定ボタン（文言は 3.9 の言語で出る）
2. **タイマー**
   - 実行中のタイマー（色の点・ラベル・残り時間・キャンセル）
   - プリセットのボタン群（折り返して並べる）
   - 自由入力: 時・分・秒の入力欄、ラベル欄、「開始」
   - 最近使ったタイマー（履歴、3 件まで表示。「もっと見る」で全件）
3. **アラーム**
   - 一覧（スイッチ・色の点・時刻・ラベル・繰り返し。クリックで編集）
   - 「＋」で新規
   - 履歴から選ぶ（3 件まで表示。「もっと見る」で全件）
4. フッター: 「テスト表示」（カウントダウンから本番表示までを一通り見せる）、「終了」

アラームの編集画面（パネル内で切り替え）: 時刻、ラベル、色、「詳細」を開くと繰り返し。保存／キャンセル／削除。

設定画面（パネル内で切り替え）: 3.8 の項目。

## 5. 動作の細目

- 判定は 0.5 秒ごとに行う。「前回の判定時刻 < 設定時刻 − カウントダウン秒数 ≤ 今回」でカウントダウン開始、「設定時刻 ≤ 今回」で本番表示
- **スリープや停止からの復帰**で設定時刻を過ぎていた場合はカウントダウンを飛ばして本番表示を出す。経過時間を表示する。複数日分を取りこぼしていても鳴らすのは 1 回
- アプリ起動時、起動前に過ぎていたアラームは鳴らさない（起動時刻を「前回の判定時刻」とする）。実行中だったタイマーが起動前に終了していた場合は、起動直後に本番表示を出す
- カウントダウン中に別のアラーム／タイマーが設定時刻を迎えた場合は同じ画面に相乗りする
- ディスプレイの接続・切断があったら、表示中のウィンドウを作り直す
- データ（アラーム・タイマー・プリセット・履歴・設定）は `UserDefaults` に JSON で保存。同期はしない

## 6. 技術方針

- SwiftUI + AppKit。`MenuBarExtra`（window スタイル）でパネル、`NSPanel` でオーバーレイ
- オーバーレイは `nonactivatingPanel` かつ `canBecomeKey = false`。`level = .screenSaver`、`collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .stationary]`。フルスクリーンのアプリや別の Space の上にも出る
- カウントダウンの表示ウィンドウは `ignoresMouseEvents = true`（クリックを下に通す）。「停止」ボタンだけ別の小さな非アクティブ化パネルに置き、そこだけクリックを受ける
- 本番表示のウィンドウはクリックを受けるが、やはりアプリを前面化しない
- キーボードの監視（ローカル・グローバルとも）は一切行わない
- Xcode プロジェクトは `project.yml` から `xcodegen generate` で生成。ソースは `VisualAlarm/` 配下
- App Sandbox 有効（ファイル・ネットワークに触らないので追加の entitlement は不要）。Mac App Store と GitHub（Developer ID 公証）の両方に同じビルド設定で出す

## 7. 対象外

- 音、通知センター、バッジ
- iCloud 同期、複数 Mac 間の共有
- キーボードショートカットによる操作
- 有料販売、アプリ内課金
